{
    "$schema": "http://iglucentral.com/schemas/com.snowplowanalytics.self-desc/schema/jsonschema/1-0-0#",
    "description": "An Location Context that describes an interactive element (like a link, button, icon), that the user can press and will trigger an Interactive Event.",
    "self": {
        "vendor": "io.objectiv.context",
        "name": "PressableContext",
        "format": "jsonschema",
        "version": "1-0-0"
    },
    "type": "object",
    "properties": {
        "id": {
            "type": [
                "string"
            ],
            "description": "A unique string identifier to be combined with the Context Type (`_type`) for Context instance uniqueness."
        },
        "_types": {
            "type": [
                "array"
            ],
            "description": "An ordered list of the parents of this Event, itself included as the last element.",
            "items": {
                "type": "string"
            }
        }
    },
    "additionalProperties": true,
    "required": [
        "id",
        "_types"
    ]
}